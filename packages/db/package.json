{
  "name": "@wcag-ai/db",
  "version": "1.0.0",
  "description": "Database schema and client for WCAG AI Platform",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "db:migrate": "prisma migrate dev",
    "db:reset": "prisma migrate reset"
  },
  "keywords": [
    "prisma",
    "database"
  ],
  "author": "WCAGAI Team",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^5.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "prisma": "^5.11.0",
    "typescript": "^5.3.3"
  }
}
